name: CD

on:
  workflow_dispatch:  # 수동 트리거만 허용

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment instructions
        run: |  
          echo "=========================================="  
          echo "이 프로젝트는 대용량 ML 의존성으로 인해"  
          echo "GitHub Actions에서 자동 배포가 불가능합니다"  
          echo "=========================================="  
          echo ""  
          echo "로컬에서 빌드 및 배포하는 방법:"  
          echo ""  
          echo "1. 로컬에서 빌드:"  
          echo "   docker compose build"  
          echo ""  
          echo "2. Docker Hub에 태그 및 푸시:"  
          echo "   docker tag pgvector-app:latest your-username/dream-diary:latest"  
          echo "   docker push your-username/dream-diary:latest"  
          echo ""  
          echo "3. 프로덕션 서버에서 배포:"  
          echo "   docker pull your-username/dream-diary:latest"  
          echo "   docker compose up -d"  
          echo ""  
          echo "=========================================="